services:
  - type: web
    name: index-trading-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: ./run_dashboard.sh
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
      - key: STREAMLIT_SERVER_PORT
        sync: false
    autoDeploy: false
